import { ICommandList } from "../types/commands.js";

import * as example from "./example.js";
import * as suggest from "./suggest.js";
// import * as autreCommande from "./autreCommande";

export const commands: ICommandList = {
    example,
    suggest,
    //  autreCommande
};
